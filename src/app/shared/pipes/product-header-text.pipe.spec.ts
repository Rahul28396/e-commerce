import { ProductHeaderTextPipe } from './product-header-text.pipe';

describe('ProductHeaderTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductHeaderTextPipe();
    expect(pipe).toBeTruthy();
  });
});
